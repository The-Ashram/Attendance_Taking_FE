import {Button, Title, Wrapper} from "@/app/resident/components/Header/styled";

export default function Header() {
    return <Wrapper>
        <Title>Hello nameeee</Title>
        <Button>Account</Button>
        <Button>Log Out</Button>
    </Wrapper>

}